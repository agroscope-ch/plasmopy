# Copy this file to config/secrets.yaml and fill in your actual values.
# config/secrets.yaml is gitignored and must never be committed.

input_data:
  spore_counts_api_query: https://your-spore-api-endpoint.example.com/data.json
  weather_api_query: https://your-weather-api-endpoint.example.com/data.json
  spore_counts_graph: null   # URL to an HTML page with the spore counts graph (shown via "Spores Mildiou" button)

site:
  latitude: 0.0       # decimal degrees
  longitude: 0.0      # decimal degrees
  elevation: 0.0      # metres above sea level
  timezone: Europe/Zurich   # IANA timezone string
